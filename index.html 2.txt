/* =========================================================
   HEERENVEEN ROLEPLAY
   PREMIUM BLUE / GLASS / DEPTH DESIGN
========================================================= */

*{
    box-sizing:border-box;
    margin:0;
    padding:0;
}

:root{
    --blue:#0637E8;
    --blue-2:#1455F5;
    --blue-3:#2D78FF;
    --blue-light:#61B8FF;
    --blue-dark:#03237F;

    --bg:#E8EEFC;
    --bg-2:#F5F8FF;

    --white:#FFFFFF;
    --white-soft:rgba(255,255,255,.72);

    --text:#101A2E;
    --text-2:#52617A;
    --text-3:#7D899D;

    --border:rgba(166,184,220,.45);
    --border-blue:rgba(6,55,232,.22);

    --shadow:
        0 25px 80px rgba(20,45,100,.12);

    --shadow-soft:
        0 12px 35px rgba(20,45,100,.09);

    --shadow-blue:
        0 20px 60px rgba(6,55,232,.20);
}


/* =========================================================
   BODY
========================================================= */

html{
    scroll-behavior:smooth;
}

body{
    min-height:100vh;

    font-family:
        "Segoe UI",
        Inter,
        Arial,
        sans-serif;

    color:var(--text);

    background:

        /* top blue atmosphere */
        radial-gradient(
            ellipse 900px 500px at 50% -120px,
            rgba(6,55,232,.30),
            transparent 70%
        ),

        /* left glow */
        radial-gradient(
            circle 500px at -10% 25%,
            rgba(45,120,255,.15),
            transparent 70%
        ),

        /* right glow */
        radial-gradient(
            circle 550px at 110% 65%,
            rgba(97,184,255,.13),
            transparent 70%
        ),

        linear-gradient(
            180deg,
            #E4EBFB 0%,
            #F5F8FF 38%,
            #EAF0FC 100%
        );

    overflow-x:hidden;
}


/* =========================================================
   BACKGROUND GRID / DEPTH
========================================================= */

body::before{
    content:"";

    position:fixed;
    inset:0;

    background-image:
        linear-gradient(
            rgba(6,55,232,.025) 1px,
            transparent 1px
        ),
        linear-gradient(
            90deg,
            rgba(6,55,232,.025) 1px,
            transparent 1px
        );

    background-size:42px 42px;

    mask-image:
        linear-gradient(
            to bottom,
            black,
            transparent 85%
        );

    pointer-events:none;
    z-index:-2;
}


/* atmospheric center glow */

body::after{
    content:"";

    position:fixed;

    width:850px;
    height:850px;

    left:50%;
    top:35%;

    transform:translate(-50%,-50%);

    background:
        radial-gradient(
            circle,
            rgba(6,55,232,.045),
            transparent 68%
        );

    pointer-events:none;

    z-index:-1;
}


/* =========================================================
   CONTAINER
========================================================= */

.container{
    width:min(1200px, calc(100% - 32px));

    margin:auto;

    padding:
        55px 0
        60px;
}


/* =========================================================
   HERO
========================================================= */

.header{
    position:relative;

    text-align:center;

    padding:
        35px
        25px
        48px;

    margin-bottom:30px;

    border-radius:32px;

    background:
        linear-gradient(
            145deg,
            rgba(255,255,255,.90),
            rgba(239,245,255,.70)
        );

    border:1px solid
        rgba(255,255,255,.95);

    box-shadow:
        0 30px 90px rgba(25,55,115,.14),
        inset 0 1px 0 rgba(255,255,255,.95);

    backdrop-filter:blur(22px);

    overflow:hidden;
}


/* large background glow */

.header::before{
    content:"";

    position:absolute;

    width:700px;
    height:400px;

    top:-330px;
    left:50%;

    transform:translateX(-50%);

    background:
        radial-gradient(
            ellipse,
            rgba(6,55,232,.28),
            rgba(45,120,255,.08) 40%,
            transparent 72%
        );

    pointer-events:none;
}


/* subtle lower glow */

.header::after{
    content:"";

    position:absolute;

    width:500px;
    height:120px;

    bottom:-90px;
    left:50%;

    transform:translateX(-50%);

    background:
        radial-gradient(
            ellipse,
            rgba(6,55,232,.30),
            transparent 70%
        );

    filter:blur(12px);

    pointer-events:none;
}


/* =========================================================
   LOGO
========================================================= */

.logo{
    position:relative;
    z-index:2;

    width:145px;
    height:112px;

    object-fit:contain;

    display:block;

    margin:0 auto 8px;

    filter:
        drop-shadow(
            0 8px 12px
            rgba(6,55,232,.22)
        )
        drop-shadow(
            0 0 28px
            rgba(45,120,255,.28)
        );

    animation:
        logoFloat 4s ease-in-out infinite;
}

@keyframes logoFloat{
    0%,100%{
        transform:translateY(0);
    }

    50%{
        transform:translateY(-6px);
    }
}


/* =========================================================
   HERO TITLE
========================================================= */

h1{
    position:relative;
    z-index:2;

    font-size:clamp(34px,5vw,54px);

    font-weight:850;

    letter-spacing:-1.8px;

    margin-bottom:11px;

    background:
        linear-gradient(
            135deg,
            #021C68 0%,
            #0637E8 48%,
            #2D78FF 100%
        );

    -webkit-background-clip:text;
    background-clip:text;

    color:transparent;

    text-shadow:
        0 8px 30px
        rgba(6,55,232,.12);
}


.subtitle{
    position:relative;
    z-index:2;

    color:var(--text-2);

    font-size:16px;

    letter-spacing:.2px;
}


/* =========================================================
   HERO LABEL
========================================================= */

.header-label{
    position:relative;
    z-index:2;

    display:inline-flex;

    align-items:center;
    gap:9px;

    margin-top:20px;

    padding:8px 15px;

    border-radius:50px;

    background:
        linear-gradient(
            135deg,
            rgba(6,55,232,.12),
            rgba(45,120,255,.055)
        );

    border:1px solid
        rgba(6,55,232,.16);

    box-shadow:
        inset 0 1px 0
        rgba(255,255,255,.8),
        0 8px 25px
        rgba(6,55,232,.07);

    color:var(--blue-dark);

    font-size:11px;

    font-weight:800;

    text-transform:uppercase;

    letter-spacing:1.3px;
}

.header-label span{
    width:7px;
    height:7px;

    border-radius:50%;

    background:var(--blue);

    box-shadow:
        0 0 0 4px rgba(6,55,232,.08),
        0 0 14px rgba(6,55,232,.8);

    animation:pulse 2s infinite;
}

@keyframes pulse{
    0%,100%{
        opacity:1;
        transform:scale(1);
    }

    50%{
        opacity:.55;
        transform:scale(.72);
    }
}


/* =========================================================
   MAIN CARDS
========================================================= */

.card{
    position:relative;

    margin-bottom:27px;

    padding:30px;

    border-radius:25px;

    background:
        linear-gradient(
            145deg,
            rgba(255,255,255,.91),
            rgba(247,250,255,.76)
        );

    border:1px solid
        rgba(255,255,255,.92);

    box-shadow:
        0 25px 70px rgba(24,52,110,.11),
        inset 0 1px 0 rgba(255,255,255,.95);

    backdrop-filter:blur(20px);

    overflow:hidden;

    transition:
        transform .28s ease,
        box-shadow .28s ease;
}


/* top shine */

.card::after{
    content:"";

    position:absolute;

    width:500px;
    height:220px;

    right:-260px;
    top:-170px;

    background:
        radial-gradient(
            ellipse,
            rgba(45,120,255,.11),
            transparent 68%
        );

    pointer-events:none;
}


/* left accent */

.card::before{
    content:"";

    position:absolute;

    top:22px;
    bottom:22px;
    left:0;

    width:4px;

    border-radius:
        0 8px 8px 0;

    background:
        linear-gradient(
            180deg,
            var(--blue-dark),
            var(--blue),
            var(--blue-light)
        );

    box-shadow:
        0 0 18px
        rgba(6,55,232,.25);
}


.card:hover{
    transform:translateY(-2px);

    box-shadow:
        0 30px 85px rgba(20,48,110,.15),
        inset 0 1px 0 rgba(255,255,255,.95);
}


/* =========================================================
   SECTION HEADINGS
========================================================= */

h2{
    position:relative;

    display:flex;

    align-items:center;
    gap:11px;

    margin-bottom:21px;

    font-size:28px;

    font-weight:850;

    letter-spacing:-.6px;

    color:var(--blue-dark);
}


/* heading line */

h2::after{
    content:"";

    flex:1;

    height:1px;

    margin-left:12px;

    background:
        linear-gradient(
            90deg,
            rgba(6,55,232,.20),
            rgba(6,55,232,.04),
            transparent
        );
}


/* =========================================================
   TEXT
========================================================= */

p,
li{
    color:var(--text-2);

    line-height:1.72;
}

strong{
    color:var(--blue-dark);
}

ul{
    margin-top:8px;
    padding-left:21px;
}


/* =========================================================
   INTRO
========================================================= */

.intro{
    max-width:920px;

    margin:
        0 auto 8px;

    text-align:center;

    font-size:15px;
}


/* =========================================================
   RANK GRID
========================================================= */

.rank-grid{
    display:grid;

    grid-template-columns:
        repeat(
            auto-fit,
            minmax(290px,1fr)
        );

    gap:17px;
}


/* =========================================================
   RANK CARD
========================================================= */

.rank{
    position:relative;

    padding:21px;

    border-radius:19px;

    background:
        linear-gradient(
            145deg,
            rgba(255,255,255,.94),
            rgba(241,246,255,.84)
        );

    border:1px solid
        rgba(192,207,235,.55);

    box-shadow:
        0 8px 25px
        rgba(25,55,115,.055),

        inset 0 1px 0
        rgba(255,255,255,.95);

    overflow:hidden;

    transition:
        transform .24s ease,
        border-color .24s ease,
        box-shadow .24s ease;
}


/* corner glow */

.rank::before{
    content:"";

    position:absolute;

    width:180px;
    height:180px;

    right:-110px;
    top:-110px;

    border-radius:50%;

    background:
        radial-gradient(
            circle,
            rgba(6,55,232,.12),
            transparent 68%
        );

    transition:.3s ease;
}


/* left status line */

.rank::after{
    content:"";

    position:absolute;

    left:0;
    top:20px;
    bottom:20px;

    width:3px;

    border-radius:
        0 5px 5px 0;

    background:
        linear-gradient(
            180deg,
            var(--blue),
            var(--blue-light)
        );

    opacity:.65;
}


.rank:hover{
    transform:
        translateY(-5px)
        scale(1.008);

    border-color:
        rgba(6,55,232,.30);

    box-shadow:
        0 20px 45px
        rgba(6,55,232,.12),

        inset 0 1px 0
        rgba(255,255,255,.95);
}


.rank:hover::before{
    transform:scale(1.5);

    background:
        radial-gradient(
            circle,
            rgba(6,55,232,.17),
            transparent 68%
        );
}


/* =========================================================
   RANK NUMBER
========================================================= */

.rank-number{
    position:relative;
    z-index:2;

    display:inline-flex;

    align-items:center;
    justify-content:center;

    min-width:40px;
    height:26px;

    padding:0 9px;

    border-radius:8px;

    background:
        linear-gradient(
            135deg,
            rgba(6,55,232,.14),
            rgba(45,120,255,.06)
        );

    border:1px solid
        rgba(6,55,232,.13);

    box-shadow:
        inset 0 1px 0
        rgba(255,255,255,.7);

    color:var(--blue);

    font-size:11px;

    font-weight:900;

    letter-spacing:1px;

    margin-bottom:8px;
}


/* =========================================================
   RANK TITLE
========================================================= */

.rank-title{
    position:relative;
    z-index:2;

    color:var(--text);

    font-size:18px;

    font-weight:850;

    margin-bottom:14px;
}


/* =========================================================
   PERMISSIONS
========================================================= */

.permission{
    position:relative;
    z-index:2;

    display:flex;

    align-items:flex-start;

    gap:8px;

    margin:7px 0;

    padding-bottom:6px;

    font-size:14px;

    line-height:1.55;

    color:var(--text-2);

    border-bottom:
        1px solid
        rgba(150,170,205,.16);
}


.permission:last-child{
    border-bottom:none;
}


.permission strong{
    white-space:nowrap;

    color:var(--blue-dark);

    font-weight:750;
}


.permission.none{
    color:#8995A8;
}


.permission.none strong{
    color:#77849A;
}


/* =========================================================
   REQUIREMENTS
========================================================= */

.requirements{
    display:grid;

    grid-template-columns:
        repeat(
            auto-fit,
            minmax(250px,1fr)
        );

    gap:17px;
}


.requirement{
    position:relative;

    padding:22px;

    border-radius:19px;

    background:
        linear-gradient(
            145deg,
            rgba(255,255,255,.94),
            rgba(240,245,255,.84)
        );

    border:1px solid
        rgba(192,207,235,.55);

    box-shadow:
        0 8px 25px
        rgba(25,55,115,.05);

    transition:
        transform .24s ease,
        box-shadow .24s ease,
        border-color .24s ease;
}


.requirement::before{
    content:"";

    position:absolute;

    left:0;
    top:18px;
    bottom:18px;

    width:3px;

    border-radius:
        0 5px 5px 0;

    background:
        linear-gradient(
            180deg,
            var(--blue),
            var(--blue-light)
        );
}


.requirement:hover{
    transform:translateY(-4px);

    border-color:
        rgba(6,55,232,.28);

    box-shadow:
        0 18px 40px
        rgba(6,55,232,.10);
}


.requirement h3{
    color:var(--blue-dark);
    font-weight:850;
}


.requirement li::marker{
    color:var(--blue);
}


/* =========================================================
   WARNING
========================================================= */

.warning{
    background:
        linear-gradient(
            125deg,
            rgba(6,55,232,.10),
            rgba(255,255,255,.90) 48%,
            rgba(235,242,255,.82)
        );

    border:
        1px solid
        rgba(6,55,232,.15);

    box-shadow:
        0 25px 65px
        rgba(6,55,232,.10);
}


.warning::after{
    width:600px;
    height:300px;

    right:-300px;
    top:-200px;

    background:
        radial-gradient(
            ellipse,
            rgba(6,55,232,.16),
            transparent 70%
        );
}


.warning h2{
    color:var(--blue-dark);
}


/* =========================================================
   FOOTER
========================================================= */

.footer{
    position:relative;

    text-align:center;

    margin-top:38px;

    padding:20px;

    color:var(--text-3);

    font-size:13px;
}


.footer::before{
    content:"";

    display:block;

    width:120px;
    height:1px;

    margin:
        0 auto 18px;

    background:
        linear-gradient(
            90deg,
            transparent,
            rgba(6,55,232,.30),
            transparent
        );
}


.footer strong{
    color:var(--blue);
}


/* =========================================================
   SCROLLBAR
========================================================= */

::-webkit-scrollbar{
    width:9px;
}

::-webkit-scrollbar-track{
    background:#E2E8F5;
}

::-webkit-scrollbar-thumb{
    background:
        linear-gradient(
            180deg,
            var(--blue),
            var(--blue-light)
        );

    border-radius:20px;

    border:
        2px solid #E2E8F5;
}


/* =========================================================
   MOBILE
========================================================= */

@media(max-width:700px){

    .container{
        width:calc(100% - 22px);

        padding:
            20px 0
            32px;
    }

    .header{
        padding:
            28px
            15px
            40px;

        border-radius:25px;

        margin-bottom:19px;
    }

    .logo{
        width:110px;
        height:88px;
    }

    h1{
        font-size:34px;
        letter-spacing:-1px;
    }

    .subtitle{
        font-size:14px;
        line-height:1.5;
    }

    .header-label{
        font-size:10px;
        letter-spacing:.8px;
    }

    .card{
        padding:23px 17px;

        border-radius:20px;

        margin-bottom:19px;
    }

    h2{
        font-size:23px;
        line-height:1.25;
    }

    h2::after{
        display:none;
    }

    .rank-grid{
        grid-template-columns:1fr;
        gap:13px;
    }

    .rank{
        padding:19px;
    }

    .requirements{
        grid-template-columns:1fr;
        gap:13px;
    }

    .intro{
        font-size:14px;
    }
}


/* =========================================================
   VERY SMALL PHONES
========================================================= */

@media(max-width:400px){

    h1{
        font-size:29px;
    }

    h2{
        font-size:21px;
    }

    .permission{
        font-size:13px;
    }

    .rank-title{
        font-size:17px;
    }
}
